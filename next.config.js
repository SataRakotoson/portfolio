/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com'], // Ajoutez ici les domaines autorisés pour les images
  },
}

module.exports = nextConfig 